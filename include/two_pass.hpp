#ifndef __TWOPASS__
#define __TWOPASS__

#include <map>

class TwoPassAlgorithm {
    // Define se descrções serão impressas
    // const bool verbose;
    // // Armazena um dicionário das definições de instruções
    // std::map<std::string, int> instruction_table;
    // // Armazena um dicionário das definições de símbolos
    // std::map<std::string, int> symbol_table;
    // // Dicionário de diretivas de préprocessamento para suas rotinas
    // // std::map<std::string, void(*)(std::vector<asm_line>::iterator&, Preprocesser*)> directive_table;

    // // Tenta acessar o valor atribuído ao símbolo pela tabela de símbolos. Se não houver, lança uma exceção
    // int resolve_symbol(std::string symbol);

    // public:
    // // Recebe um arquivo e cria um novo arquivo .OBJ, com o código montado
    // void assemble(std::string, bool print = false);
    // // Construtor
    // TwoPassAlgorithm(bool verbose = false);
};

#endif